/**
 *
 * 1. Create a firebase project
 * 2. Create web app
 * 3. npm i firebase
 * 4. Save firebase config and export default app
 * 5. Build > Authentication > Get Started > Enable sign in method
 * 6. Create Sign up and Log in route
 *
 **/

/**
 *
 * 1. Create a context provider (AuthProvider)
 * 2. Create a context and set provider
 * 3. Set the children props
 * 4. Set context value
 * 5. Set the provider
 *
 **/

/**
 *
 * HOSTING
 * 1 time per pc
 * 1. npm install -g firebase-tools
 * 2. firebase login
 *
 **/

/**
 *
 * for each project one time
 * 1. firebase init
 * 2. proceed
 * 3. hosting: firebase (up and down arrow), use space bar to select
 * 4. existing project
 * 5. select the project carefully
 * 6. which project as public directory : dist(for vite)
 * 7. single page application: yes
 * 8. continuous deployment: no
 *
 * */

/**
 * for each project deploy:
 * 1. npm run build
 * 2. firebase deploy
 * */
